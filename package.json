{
  "name": "sv-travels",
  "version": "1.0.0",
  "description": "SV Travels - Bus & Tempo Traveller Services",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && node server.js",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["travel", "bus", "tempo-traveller"],
  "author": "SV Travels",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
